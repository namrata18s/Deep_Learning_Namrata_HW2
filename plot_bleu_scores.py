import matplotlib.pyplot as plt

# Assume bleu_scores is a list of BLEU scores for each video in the test set
# And video_ids is a list of corresponding video IDs
# You should have computed bleu_scores earlier

plt.figure(figsize=(14, 5))
plt.bar

